# Cost Split Application

The backend MVP is in [`backend`](./backend). It is a Spring Boot and Kotlin REST API for creating users and groups, recording shared expenses, splitting prices equally, and calculating balances and suggested repayments.

See [`backend/README.md`](./backend/README.md) for the database design, API endpoints, and local setup.

